import hashlib
print(hashlib.sha256("admin123".encode()).hexdigest())

import sqlite3

conn = sqlite3.connect('users.db')
cursor = conn.cursor()

cursor.execute("DELETE FROM users")
cursor.execute("INSERT INTO users VALUES (?, ?)", ("admin", "PASTE_HASH_HERE"))

conn.commit()
conn.close()
exit()
